server:
  host: "0.0.0.0"
  port: 8080
  mode: "web"
  public_url: ""     # 后端真实地址，留空则自动使用请求来源地址

auth:
  password: "byteshare"

upload:
  dir: "./uploads"
  max_size: 500  # MB
  default_expiry: 3  # days
  max_expiry: 30  # days

database:
  path: "./byteshare.json"
